# Web framework
fastapi==0.115.0
uvicorn==0.32.0          # no [standard] extra — uvloop removed for local dev

# LangGraph orchestration
langgraph==0.2.55
langchain-core==0.3.21

# Google Gemini (POC LLM + Vision + OCR — free tier)
google-generativeai==0.8.3

# Database (Supabase free tier)
supabase==2.10.0

# Settings & validation
pydantic==2.9.2
pydantic-settings==2.6.1

# Image processing (pHash fraud check — T-012)
imagehash==4.3.1
Pillow==11.0.0

# QR code generation (T-018)
qrcode[pil]==8.0

# HTTP client
httpx==0.27.2

# YAML config loader
PyYAML==6.0.2

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0

# Code quality
black==24.10.0
isort==5.13.2
